import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Domaincard = () => {
  return (
    <View>
      <Text style={{ color: "white" }}>Domaincard</Text>
    </View>
  );
};

export default Domaincard;

const styles = StyleSheet.create({});
